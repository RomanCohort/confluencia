% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{cf_epitope_train}
\alias{cf_epitope_train}
\title{Train an epitope model from CSV}
\usage{
cf_epitope_train(
  csv_path,
  sequence_col = "sequence",
  target_col = "binding",
  env_cols = NULL,
  model_name = "hgb",
  test_size = 0.2,
  random_state = 42,
  save_path = NULL
)
}
\arguments{
\item{csv_path}{Path to the training CSV file}

\item{sequence_col}{Column name containing peptide sequences (default: "sequence")}

\item{target_col}{Column name containing target values (default: "binding")}

\item{env_cols}{Optional list of environmental parameter column names}

\item{model_name}{Model type: "ridge", "rf", "hgb", "mlp" (default: "hgb")}

\item{test_size}{Fraction of data to use for validation (default: 0.2)}

\item{random_state}{Random seed for reproducibility (default: 42)}

\item{save_path}{Path to save the .joblib bundle (default: alongside CSV)}
}
\value{
Named list with metrics: mae, rmse, r2, n_train, n_val, bundle_path
}
\description{
Trains an epitope binding prediction model from a CSV file and saves
the model bundle as a .joblib file for later use with cf_epitope_predict().
}
\examples{
\dontrun{
# Train from a CSV file
result <- cf_epitope_train("my_epitope_data.csv", model_name = "hgb")
print(result$bundle_path)  # Path to saved model

# Use the trained model for prediction
pred <- cf_epitope_predict(result$bundle_path, "SLYNTVATL")
}
}
