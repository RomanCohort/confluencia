% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hub.R
\name{cf_hub_verify_model}
\alias{cf_hub_verify_model}
\title{Promote a model's verification tier}
\usage{
cf_hub_verify_model(
  model_id,
  level,
  reviewer = "",
  evidence_url = "",
  circ_casp_metrics = NULL
)
}
\arguments{
\item{model_id}{The hub model ID to verify}

\item{level}{Target tier: "reproducible", "verified", or "benchmark_top"}

\item{reviewer}{ORCID or name of the verifier (for audit trail)}

\item{evidence_url}{URL supporting the claim (e.g., Circ-CASP results page)}

\item{circ_casp_metrics}{Named list of competition metrics (e.g., list(rmsd=8.2, rank=3))}
}
\value{
Named list with updated verification_level and audit info
}
\description{
Tiers (low -> high): unverified < reproducible < verified < benchmark_top.
A tier can only be raised, never lowered, by this method.
}
\examples{
\dontrun{
cf_hub_verify_model("hub:circRNA:0000-0002-1825-0097:abc123def456",
                    level = "verified",
                    reviewer = "circ-casp-committee",
                    circ_casp_metrics = list(rmsd = 8.2, rank = 3))
}
}
