% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{cf_drug_train}
\alias{cf_drug_train}
\title{Train a drug model from CSV}
\usage{
cf_drug_train(
  csv_path,
  smiles_col = "smiles",
  target_col = "efficacy",
  env_cols = NULL,
  model_name = "gbr",
  test_size = 0.2,
  random_state = 42,
  save_path = NULL
)
}
\arguments{
\item{csv_path}{Path to the training CSV file}

\item{smiles_col}{Column name containing SMILES strings (default: "smiles")}

\item{target_col}{Column name containing target values (default: "efficacy")}

\item{env_cols}{Optional list of environmental parameter column names}

\item{model_name}{Model type: "ridge", "rf", "gbr", "hgb", "mlp" (default: "gbr")}

\item{test_size}{Fraction of data to use for validation (default: 0.2)}

\item{random_state}{Random seed for reproducibility (default: 42)}

\item{save_path}{Path to save the .joblib bundle (default: alongside CSV)}
}
\value{
Named list with metrics: mae, rmse, r2, n_train, n_val, bundle_path
}
\description{
Trains a drug efficacy prediction model from a CSV file and saves
the model bundle as a .joblib file for later use with cf_drug_predict().
}
\examples{
\dontrun{
# Train from a CSV file
result <- cf_drug_train("my_drug_data.csv", model_name = "ridge")
print(result$bundle_path)  # Path to saved model

# Use the trained model for prediction
pred <- cf_drug_predict(result$bundle_path, "CC(=O)Oc1ccccc1C(=O)O")
}
}
