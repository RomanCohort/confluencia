% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctm-simulate.R
\name{cf_ctm_simulate}
\alias{cf_ctm_simulate}
\title{Simulate small-molecule PK (CTM 6-compartment model)}
\usage{
cf_ctm_simulate(
  dose,
  freq,
  params = NULL,
  binding = 0.5,
  immune = 0.5,
  inflammation = 0.5,
  horizon = 72
)
}
\arguments{
\item{dose}{Dose in mg}

\item{freq}{Dosing frequency per day}

\item{params}{Named numeric vector from cf_ctm_params(), or NULL to auto-derive}

\item{binding}{Binding affinity score (used if params is NULL)}

\item{immune}{Immune activation score (used if params is NULL)}

\item{inflammation}{Inflammation score (used if params is NULL)}

\item{horizon}{Simulation horizon in hours}
}
\value{
data.frame with columns: time_h, absorption_A, distribution_D,
effect_E, metabolism_M, efficacy_signal, toxicity_signal
}
\description{
Simulates the 6-compartment pharmacokinetic model for small molecules.
}
\examples{
cf_ctm_simulate(dose = 200, freq = 2, binding = 0.72, immune = 0.65)
}
