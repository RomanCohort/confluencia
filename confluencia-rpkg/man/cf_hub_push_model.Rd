% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hub.R
\name{cf_hub_push_model}
\alias{cf_hub_push_model}
\title{Share a trained model with the community (federated)}
\usage{
cf_hub_push_model(
  bundle_path,
  metadata = NULL,
  uploader = "anonymous",
  license = "MIT",
  strip_env_medians = FALSE
)
}
\arguments{
\item{bundle_path}{Path to the .joblib model bundle}

\item{metadata}{Named list of model metadata (e.g., list(model_name="ridge", r2=0.91))}

\item{uploader}{Uploader identifier (default: "anonymous")}

\item{license}{License for the shared model (default: "MIT")}

\item{strip_env_medians}{Remove env_medians to eliminate training data traces (default: FALSE)}
}
\value{
Named list with model_id and message
}
\description{
Uploads a .joblib model bundle to the Confluencia Hub.
Only the model weights are shared — raw training data is never exposed.
Set strip_env_medians=TRUE to also remove statistical traces of training data.
}
\examples{
\dontrun{
# Share with full privacy: strip statistical traces
result <- cf_hub_push_model("my_drug_bundle.joblib",
                             metadata = list(model_name = "ridge", r2 = 0.91),
                             strip_env_medians = TRUE)
print(result$model_id)
}
}
